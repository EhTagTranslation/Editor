import { GithubIndentityGuard } from './github-indentity.guard';

describe('GithubIndentityGuard', () => {
  it('should be defined', () => {
    expect(new GithubIndentityGuard()).toBeDefined();
  });
});
